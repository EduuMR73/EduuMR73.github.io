---
context: default
phase: portfolio-f8-qa
task: 1
total_tasks: 1
status: not_started
last_updated: 2026-06-12T06:29:10.958Z
---

<current_state>
F8 — QA final pendiente. Todo lo anterior completado y commiteado. Árbol limpio.
Últimas tareas esta sesión: README del portfolio (commit 1bd5c28) + meta tag Google Search Console (commit c9d5b60).
</current_state>

<completed_work>

Sesiones anteriores:
- F0–F7 completos, lightbox nativo `<dialog>`, thumbnails Emite con data-full
- Fachada de vídeo (SBL, Pokédex, Emite) con SVG inline para ▶
- JS/no-JS reveals (clase `js` en `<html>`, CSS gateado con `.js`)
- Google Fonts non-blocking, contraste footer A11y (7.3:1 ✓)
- Copy tarjetas, hamparo-cover descomentado
- Fix lightbox: `#lightbox[open] { display: flex }` (espacio vacío post-footer)

Esta sesión (2026-06-12):
- README del portfolio — commit 1bd5c28
- Meta tag Google Search Console — commit c9d5b60
  (`content="7L1jCAZkfwjaxo14HvKGYJ4yjNxozRBgllYgjeSH5kw"`, línea 5 del `<head>`)
</completed_work>

<remaining_work>

**Acciones del usuario (antes de F8):**
1. Subir `assets/img/sbl-poster-vertical.webp` (fachada SBL — ruta preparada en HTML)
2. Comprimir `assets/img/emite-poster.webp` de 42 KB a ~15-20 KB (squoosh.app, quality ~80)
3. `git push origin main` para que GitHub Pages sirva la meta tag GSC
4. Verificar Google Search Console — confirmar verificación del sitio

**F8 — QA final** (checklist §11 del PLAN_PORTFOLIO.md + Lighthouse móvil):
- Cero enlaces rotos (4 repos + demo Emite + CV + mailto)
- Cero textos truncados en 360/768/1024/1440
- Sin overflow horizontal en 360px
- Stats correctas sin JS (desactivar JS y verificar)
- Lighthouse móvil: P ≥ 95 · A11y ≥ 95 · SEO 100
- Preview OG correcta en opengraph.xyz
- prefers-reduced-motion respetado
- Favicon visible en pestaña
- HTML validado (validator.w3.org)
- Probado en Chrome, Firefox y Android real
- Search Console: propiedad verificada + sitemap enviado
- Leer copy completo en voz alta (erratas)
</remaining_work>

<decisions_made>

- Fachada de vídeo: img poster siempre visible en reposo, `<video hidden>`. Click → play. `ended` → volver a fachada.
- `.card-media video:not([hidden]) { display: block }` — respeta atributo `hidden` nativo sin !important
- SVG inline para ▶ (no emoji) — render plano en iOS
- Google Fonts con `rel=preload + onload` — no blocking, noscript fallback
- Footer: text-secondary para texto corrido, text-primary para enlaces (jerarquía clicable)
- JS/no-JS: clase `js` en `<html>` vía primera línea de main.js; CSS gateado con `.js`
- Único `!important` conocido: `.nav a.nav--active:not(.btn--primary)`
</decisions_made>

<blockers>
- `sbl-poster-vertical.webp`: FALTA → fachada SBL vacía hasta que Eduardo la suba (no bloqueante para push)
- `emite-poster.webp` (42 KB): comprimir antes de F8 para pasar Lighthouse image delivery
- `emite-1/2/3-full.webp`: lightbox hace fallback a thumbnails (funcional, no crítico)
- `assets/cv/CV-Eduardo-Moreno.pdf`: pendiente de añadir
</blockers>

## Required Reading (in order)
1. `PLAN_PORTFOLIO.md` §11 — Checklist QA pre-publicación (para F8)
2. `PLAN_PORTFOLIO.md` §9 — Presupuestos de performance

## Infrastructure State
- Repo: `EduuMR73.github.io`, rama `main`, último commit `c9d5b60` — commits en local, pendiente push
- Sin servidor de dev — abrir index.html directo o `python -m http.server`
- Stack: HTML/CSS/JS plano, sin frameworks, sin build step
- Assets de vídeo en `assets/video/`: emite-demo.mp4, sbl-demo.mp4, pokedex-demo.mp4 ✓
- `sbl-poster-vertical.webp` en `assets/img/`: FALTA

<context>
Portfolio personal estático. Identidad: fondo carbón #161616, acento champán #E8C96D (Plus Jakarta Sans + JetBrains Mono).
main.js: primera línea es `document.documentElement.classList.add('js')`.
Todos los IIFEs de main.js usan `;(function(){` como patrón defensivo.
Patrón fachada: `.card-facade` (img + btn SVG) + `<video hidden>` dentro de `.card-media`.
El `<dialog id="lightbox">` está dentro de `<main>`, antes de `</main>`.
</context>

<next_action>
1. Eduardo: subir `sbl-poster-vertical.webp` a `assets/img/`
2. Eduardo: comprimir `emite-poster.webp` a ~15-20 KB
3. `git push origin main`
4. Verificar Google Search Console
5. Ejecutar checklist §11 (F8 — QA final) + Lighthouse móvil
</next_action>
