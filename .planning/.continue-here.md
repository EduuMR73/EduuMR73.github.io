---
context: default
phase: portfolio-static
status: in_progress
last_updated: 2026-06-12T03:00:16.648Z
---

<current_state>
Portfolio estático EduuMR73.github.io. Fases F0–F7 + Lightbox completados.
Pendiente únicamente F8 — QA final (checklist §11 + Lighthouse).
Repo limpio — todos los cambios commiteados.
</current_state>

<completed_work>

Esta sesión (2026-06-12):

- **Lightbox nativo** (commit 0613ef5)
  - `<dialog id="lightbox">` con button ✕ e `<img id="lightbox-img">` antes de `</main>`
  - 4 `.lightbox-trigger` en imágenes de Emite (cover + 3 thumbnails)
  - Bloque Hamparo comentado listo para activar cuando llegue hamparo-cover.webp
  - CSS: trigger reset + cursor zoom-in, dialog full-screen, backdrop 85% negro, botón ✕ fijo, lbFadeIn animation
  - JS IIFE: open/close por ✕/backdrop/Esc, foco devuelto al trigger, src limpio al cerrar

- **fix — Lightbox data-full + max-height** (commit fe6b66d)
  - `data-full="assets/img/emite-N-full.webp"` en los 3 thumbnails (cover sin data-full)
  - JS: `dlgImg.src = btn.dataset.full || btn.dataset.src` (fallback elegante)
  - CSS: `max-height: 85dvh` en `#lightbox-img` (de 90dvh → 85dvh)

Sesión anterior:
- F6 Motion + pulido, F7 SEO + OG + JSON-LD, fixes móvil, copy hero/contacto/footer
</completed_work>

<remaining_work>

- **F8 — QA final**: checklist §11 del PLAN_PORTFOLIO.md + Lighthouse móvil
  - Cero enlaces rotos (4 repos + demo + CV + mailto)
  - Cero textos truncados en 360/768/1024/1440
  - Sin overflow horizontal en 360px
  - Stats correctas sin JS
  - Lighthouse móvil: P ≥ 95 · A11y ≥ 95 · SEO 100
  - Preview OG correcta en opengraph.xyz
  - Contraste AA verificado
  - prefers-reduced-motion respetado
  - Favicon visible en pestaña
  - HTML validado (validator.w3.org)
  - Probado en Chrome, Firefox y Android real
  - Search Console: propiedad verificada + sitemap
  - Leer copy completo en voz alta (erratas)
</remaining_work>

<decisions_made>

- Lightbox con `<dialog>` nativo — sin librerías
- `data-full` para versión completa de thumbnails; fallback a `data-src` con `||`
- `dlgImg.src = ''` al cerrar — limpia la imagen para no cachear innecesario
- Cover (emite-cover.webp) no necesita data-full — ya es full-res
- Bloque Hamparo comentado in-place — activar cuando llegue hamparo-cover.webp
</decisions_made>

<blockers>
- assets/img/emite-1-full.webp, emite-2-full.webp, emite-3-full.webp no existen aún
  → el lightbox hace fallback a los thumbnails (funcional, solo sin versión full)
</blockers>

## Required Reading (in order)
1. `PLAN_PORTFOLIO.md` §11 — Checklist QA pre-publicación (para F8)

## Infrastructure State
- Repo: EduuMR73.github.io, rama main, todos los commits en local
- Sin servidor de dev — abrir index.html directo o `python -m http.server`
- git status: limpio

<context>
Portfolio estático HTML/CSS/JS puro, sin frameworks ni build step.
Identidad visual: fondo carbón #161616, acento champán #E8C96D.
Único !important: .nav a.nav--active:not(.btn--primary).
Todos los IIFEs de main.js usan ;(function(){ como patrón defensivo.
</context>

<next_action>
1. git push origin main
2. Ejecutar checklist §11 (F8 — QA final) + Lighthouse móvil
3. Añadir emite-1/2/3-full.webp cuando estén disponibles
</next_action>
