---
context: default
phase: portfolio-static
status: in_progress
last_updated: 2026-06-12T18:00:00.000Z
---

<current_state>
Portfolio estático EduuMR73.github.io. Fases F0–F7 + Lightbox completados.
Esta sesión cerró la conexión de todos los assets de vídeo/imagen y una
batería de fixes de calidad (Lighthouse, A11y, JS/no-JS). Dos assets
pendientes de acción del usuario antes de lanzar F8 QA.
Repo limpio — todos los cambios commiteados.
</current_state>

<completed_work>

Esta sesión (2026-06-12, tarde):

- **Bug fix — espacio vacío post-footer**
  - `#lightbox { display: flex }` → `#lightbox[open] { display: flex }`
  - El CSS sobreescribía el `display:none` nativo del `<dialog>` cerrado

- **Conexión de material — vídeos e imágenes**
  - Emite: cover reemplazado por `<video id="emite-video" preload="metadata" poster="emite-poster.webp">`
  - Botón "▶ Vídeo (2:35)" añadido en `.emite-ctas`
  - SBL y Pokédex: patrón fachada (`card-facade`: img + btn▶ superpuesto; `<video hidden>` debajo)
  - SBL usa `assets/img/sbl-poster-vertical.webp` (ruta preparada — asset pendiente)
  - Pokédex usa `assets/img/pokedex-poster.webp` existente
  - Hamparo: cover descomentado (`hamparo-cover.webp` ya disponible)
  - CSS: `.card-media` 400px fijo, img `cover`, video `contain` via `:not([hidden])`
  - JS: IIFE fachada (click → show video + play; `ended` → show facade); IIFE Emite scroll+play; IntersectionObserver pausa vídeos al salir del viewport

- **Copy tarjetas del grid**
  - Hamparo, SBL y Pokédex con descripciones nuevas (producto, no técnica)

- **Fix multiplataforma — ▶ emoji → SVG inline**
  - 3 ocurrencias reemplazadas: botón Emite (12×12), botones fachada SBL/Pokédex (20×20)
  - `fill="currentColor"` — se renderiza plano en iOS, no como emoji azul

- **Lighthouse Performance fixes**
  - Google Fonts non-blocking: `rel="preload" as="style" onload` + `<noscript>` fallback
  - Contraste footer A11y: text-muted (3.4:1 ✗) → text-secondary (7.3:1 ✓) para texto, text-primary (~13:1) para enlaces
  - FIX 3 lazy loading: ya estaba completo desde antes

- **Fix JS/no-JS reveals**
  - `document.documentElement.classList.add('js')` como primera línea de main.js
  - CSS: `.reveal` → `.js .reveal` y `.reveal--visible` → `.js .reveal--visible`
  - Sin JS: todo visible desde el primer frame. Con JS: fade al scroll normal

Sesión anterior:
- F0–F7 completos, lightbox nativo con `<dialog>`, thumbnails Emite con data-full
</completed_work>

<remaining_work>

**Acciones del usuario (BLOQUEANTES para F8):**
1. Subir `assets/img/sbl-poster-vertical.webp` (fachada de SBL — ruta ya preparada en HTML)
2. Comprimir `assets/img/emite-poster.webp` de 42 KB a ~15-20 KB (Lighthouse image delivery)
   - Herramienta: squoosh.app o cwebp. Dimensiones: 1200×750 WebP quality ~80

**F8 — QA final** (checklist §11 del PLAN_PORTFOLIO.md + Lighthouse móvil):
- Cero enlaces rotos (4 repos + demo Emite + CV + mailto)
- Cero textos truncados en 360/768/1024/1440
- Sin overflow horizontal en 360px
- Stats correctas sin JS (desactivar JS y verificar)
- Lighthouse móvil: P ≥ 95 · A11y ≥ 95 · SEO 100
- Preview OG correcta en opengraph.xyz
- prefers-reduced-motion respetado
- Favicon visible en pestaña
- HTML validado (validator.w3.org)
- Probado en Chrome, Firefox y Android real
- Search Console: propiedad verificada + sitemap enviado
- Leer copy completo en voz alta (erratas)
</remaining_work>

<decisions_made>

- Fachada de vídeo (no toggle): img de poster siempre visible en reposo, video hidden. Click → play. ended → volver a fachada. Sin botón toggle visible en el grid.
- `.card-media video:not([hidden]) { display: block }` — patrón para respetar atributo `hidden` nativo sin !important
- SVG inline para ▶ (no emoji) — multiplicidad de plataformas
- Google Fonts con `rel=preload + onload` — no blocking. noscript fallback.
- Footer: text-secondary para texto corrido, text-primary para enlaces (jerarquía clicable)
- JS/no-JS: clase `js` en `<html>` via JS primera línea; CSS gated con `.js` para no ocultar sin JS
- sbl-poster.webp (147 KB) es huérfana — no se referencia en HTML, no afecta Lighthouse
</decisions_made>

<blockers>
- sbl-poster-vertical.webp: no existe aún → fachada SBL muestra área vacía hasta que Eduardo la suba
- emite-poster.webp (42 KB): Lighthouse la puede señalar → comprimir antes de F8
- emite-1/2/3-full.webp: lightbox hace fallback a thumbnails (funcional, no crítico)
</blockers>

## Required Reading (in order)
1. `PLAN_PORTFOLIO.md` §11 — Checklist QA pre-publicación (para F8)
2. `PLAN_PORTFOLIO.md` §9 — Presupuestos de performance

## Infrastructure State
- Repo: EduuMR73.github.io, rama main, todos los commits en local (sin push)
- Sin servidor de dev — abrir index.html directo o `python -m http.server`
- git status: limpio (nada uncommitted)
- Assets de vídeo en `assets/video/`: emite-demo.mp4, sbl-demo.mp4, pokedex-demo.mp4 ✓
- `sbl-poster-vertical.webp` en `assets/img/`: FALTA (pendiente Eduardo)

<context>
Portfolio estático HTML/CSS/JS puro, sin frameworks ni build step.
Identidad visual: fondo carbón #161616, acento champán #E8C96D (Plus Jakarta Sans + JetBrains Mono).
main.js: primera línea es `document.documentElement.classList.add('js')`.
Todos los IIFEs de main.js usan `;(function(){` como patrón defensivo.
Patrón fachada: `.card-facade` (img + btn SVG) + `<video hidden>` dentro de `.card-media`.
El `<dialog id="lightbox">` está dentro de `<main>`, antes de `</main>`.
Único !important conocido: `.nav a.nav--active:not(.btn--primary)`.
</context>

<next_action>
1. Eduardo: subir `sbl-poster-vertical.webp` a `assets/img/`
2. Eduardo: comprimir `emite-poster.webp` a ~15-20 KB
3. git push origin main
4. Ejecutar checklist §11 (F8 — QA final) + Lighthouse móvil
</next_action>
